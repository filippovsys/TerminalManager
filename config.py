# -*- coding: utf-8 -*-
"""
Конфигурация HalkTerminalManager.

DB_PATH указывает на общий файл БД на сетевой папке -- именно он должен
быть доступен всем пользователям (как в HalkAssetsGUI). Для локальной
разработки/тестов можно временно указать локальный путь.
"""

import os

# Версия приложения. Обновляется при каждом наборе значимых изменений --
# см. CHANGELOG.md рядом с исходниками. Отображается в заголовке главного
# окна и в "О программе", чтобы всегда было видно, какая версия запущена
# на конкретном компьютере.
APP_VERSION = "1.3"

# Версия схемы БД -- увеличивается при каждой структурной правке БД
# (ALTER TABLE, новая таблица, изменение VIEW). Хранится в самой БД,
# чтобы программа могла понять, какая версия базы у конкретного пользователя.
SCHEMA_VERSION = 4

# TODO: заменить на реальный сетевой путь перед развёртыванием,
# например: r"\\SERVER\HalkTerminalManager\data\terminals.db"
DB_PATH = os.environ.get("HTM_DB_PATH", os.path.join(os.path.dirname(__file__), "data", "terminals.db"))

SCHEMA_SQL_PATH = os.path.join(os.path.dirname(__file__), "db_schema.sql")
CHANGELOG_PATH = os.path.join(os.path.dirname(__file__), "CHANGELOG.md")

# Через сколько минут неактивная блокировка карточки считается "зависшей"
# и может быть снята другим пользователем (на случай, если приложение
# закрылось аварийно и не сняло блокировку само).
EDIT_LOCK_TIMEOUT_MINUTES = 30

# Параметры хеширования пароля (без внешних библиотек -- используем
# встроенный hashlib.pbkdf2_hmac, этого достаточно для внутренней программы)
PBKDF2_ITERATIONS = 200_000